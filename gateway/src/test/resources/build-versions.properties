# Filtered by Maven at test-resource-processing time (see <testResources> in gateway/pom.xml).
# The placeholder below is resolved to whatever version `jmx_prometheus_javaagent.version`
# evaluates to for THIS module -- i.e. exactly what a real `mvn package` run would bundle,
# inherited unpinned from the `common` parent POM. Read by JmxPrometheusJavaagentVersionPinTest.
jmx_prometheus_javaagent.version=${jmx_prometheus_javaagent.version}
